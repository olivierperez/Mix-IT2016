<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="myTextView">
        <item name="android:textColor">@android:color/black</item>
    </style>
    <!-- Les layouts utilises dans l'appli collent à leurs parents -->
    <style name="myLinearLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="myTableLayout">
        <item name="android:stretchColumns">*</item>
        <item name="android:shrinkColumns">*</item>
    </style>


    <style name="WhatsOnItemBase">
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">@dimen/text_size_medium</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:maxLines">2</item>
        <item name="android:paddingLeft">@dimen/whats_on_item_padding</item>
        <item name="android:paddingRight">@dimen/whats_on_item_padding</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@color/blue</item>
    </style>

    <style name="WhatsOnStaticItem" parent="WhatsOnItemBase">
        <item name="android:textSize">@dimen/text_size_medium</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:maxLines">2</item>
        <item name="android:gravity">center_vertical|center</item>
        <item name="android:textAppearance">?android:attr/textAppearanceSmall</item>

    </style>

    <style name="myCalendarTitle" parent="myTextView">
        <item name="android:textSize">@dimen/text_size_large</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>
        <item name="android:textAllCaps">true</item>
    </style>
    <style name="myCalendarTitleh2" parent="myTextView">
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">bottom</item>
        <item name="android:textAllCaps">true</item>
    </style>
</resources>
